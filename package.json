{
  "name": "acp-coding-agent-dispatcher",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Local Codex plugin for discovering coding agents and recording dispatcher jobs.",
  "scripts": {
    "start:mcp": "node ./mcp/server.mjs",
    "check": "node --check ./mcp/server.mjs",
    "smoke": "node ./scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
