{
  "name": "agent-router-mcp",
  "version": "0.7.1",
  "description": "Generic MCP server for routing coding tasks to local ACP agents. Works with any MCP client (Claude Desktop, Cursor, Windsurf, Codex, etc.).",
  "license": "MIT",
  "author": {
    "name": "peanut996"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/peanut996/agent-router-mcp.git"
  },
  "keywords": [
    "mcp",
    "acp",
    "agent",
    "router",
    "coding-agents",
    "model-context-protocol"
  ],
  "type": "module",
  "bin": {
    "agent-router-mcp": "bin/agent-router.mjs"
  },
  "main": "./mcp/server.mjs",
  "files": [
    "bin/",
    "mcp/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "start": "node ./bin/agent-router.mjs",
    "start:mcp": "node ./mcp/server.mjs",
    "check": "node --check ./bin/agent-router.mjs && node --check ./mcp/constants.mjs && node --check ./mcp/utils.mjs && node --check ./mcp/storage.mjs && node --check ./mcp/agents.mjs && node --check ./mcp/acp-client.mjs && node --check ./mcp/jobs.mjs && node --check ./mcp/server.mjs && node -e \"import('./mcp/server.mjs')\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "engines": {
    "node": ">=18"
  }
}
